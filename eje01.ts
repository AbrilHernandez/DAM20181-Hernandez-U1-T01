let nombre:string;
nombre="Abril";
let edad:number=22;

let persona = {
    nombre:nombre,
    edad:edad
}

let mensaje:string = `Mi nombre es ${persona.nombre} y tengo ${persona.edad}años`;

console.log(mensaje);